// NO-OP STUB: Event type definitions replaced with empty types for privacy
// All telemetry events have been removed

// Empty event hierarchy - no events are tracked
export type Events = Record<string, any>;

// Empty event arguments - no data is collected
export type EventArgs = Record<string, any>;
